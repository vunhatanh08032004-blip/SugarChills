@using QL_MatHangAnUong.Helpers
@using QL_MatHangAnUong.Models
@model QL_MatHangAnUong.Models.ViewModels.ChiTietSanPhamViewModel
@{
    var sp = Model.SanPham;
}

<div class="container">
    <div class="sc-breadcrumb">
        <a href="@Url.Action("Index", "Home")">@Resources.Strings.Common_TrangChu</a>
        <i class="bi bi-chevron-right" style="font-size:11px"></i>
        <a href="@Url.Action("Index", "SanPham")">@Resources.Strings.Header_ThucDon</a>
        @if (sp.LoaiSanPham != null)
        {
            <i class="bi bi-chevron-right" style="font-size:11px"></i>
            <a href="@Url.Action("Index", "SanPham", new { maLoai = sp.MaLoai })">@sp.LoaiSanPham.TenLoai</a>
        }
        <i class="bi bi-chevron-right" style="font-size:11px"></i>
        <span class="sc-text-primary fw-bold">@sp.TenSP</span>
    </div>

    <!-- ============ THÔNG TIN SẢN PHẨM ============ -->
    <div class="sc-panel">
        @using (Html.BeginForm("Them", "GioHang", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <input type="hidden" name="maSP" value="@sp.MaSP" />

            <div class="row g-4" id="khungTuyChon" data-gia-goc="@sp.GiaBanThucTe">
                <div class="col-lg-5">
                    <div class="position-relative">
                        @if (sp.PhanTramGiam > 0)
                        {
                            <span class="sc-badge-sale" style="font-size:14px">-@sp.PhanTramGiam%</span>
                        }
                        <img src="@sp.HinhAnh" class="sc-detail-img" alt="@sp.TenSP"
                             data-fallback="https://placehold.co/800x800/FFE6EE/E8517F?text=SugarChills" />
                    </div>
                </div>

                <div class="col-lg-7">
                    <div class="sc-product-cat">
                        @(sp.LoaiSanPham != null ? sp.LoaiSanPham.TenLoai : "SugarChills")
                    </div>
                    <h1 style="font-size:30px">@sp.TenSP</h1>

                    <div class="d-flex align-items-center gap-2 mb-2">
                        <span class="text-warning">
                            <i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-fill"></i><i class="bi bi-star-half"></i>
                        </span>
                        <span class="sc-text-muted small">@(string.Format(Resources.Strings.Detail_DaBanFormat, sp.LuotBan))</span>
                        @if (sp.SoLuongTon > 0)
                        {
                            <span class="sc-badge badge-success">@Resources.Strings.Detail_ConHang</span>
                        }
                        else
                        {
                            <span class="sc-badge badge-danger">@Resources.Strings.Detail_TamHet</span>
                        }
                    </div>

                    <div class="mb-3">
                        <span class="sc-price-lg">@DinhDang.Tien(sp.GiaBanThucTe)</span>
                        @if (sp.PhanTramGiam > 0)
                        {
                            <span class="sc-price-old" style="font-size:18px">@DinhDang.Tien(sp.Gia)</span>
                        }
                    </div>

                    <p class="sc-text-muted">@sp.MoTa</p>

                    <hr style="border-color:var(--sc-line)" />

                    <!-- ---- Tùy chọn SIZE (radio) ---- -->
                    <div class="mb-3">
                        <div class="sc-option-title">@Resources.Strings.Detail_ChonSize</div>
                        <div class="sc-pill-group">
                            @foreach (var size in TuyChonSanPham.CacSize)
                            {
                                decimal phuThu = TuyChonSanPham.PhuThuSize(size);
                                <label class="sc-pill">
                                    <input type="radio" name="size" value="@size" data-phu-thu="@phuThu"
                                           checked="@(size == "M")" />
                                    <span>
                                        @Resources.Strings.Detail_Size @size
                                        @if (phuThu > 0)
                                        {
                                            <text> (+@DinhDang.Tien(phuThu))</text>
                                        }
                                    </span>
                                </label>
                            }
                        </div>
                    </div>

                    <div class="row g-3 mb-3">
                        <!-- ---- Mức đường (radio) ---- -->
                        <div class="col-md-6">
                            <div class="sc-option-title">@Resources.Strings.Detail_MucDuong</div>
                            <div class="sc-pill-group">
                                @foreach (var muc in TuyChonSanPham.CacMucDuong)
                                {
                                    <label class="sc-pill">
                                        <input type="radio" name="duong" value="@muc" checked="@(muc == "100%")" />
                                        <span>@muc</span>
                                    </label>
                                }
                            </div>
                        </div>

                        <!-- ---- Mức đá (radio) ---- -->
                        <div class="col-md-6">
                            <div class="sc-option-title">@Resources.Strings.Detail_MucDa</div>
                            <div class="sc-pill-group">
                                @foreach (var muc in TuyChonSanPham.CacMucDa)
                                {
                                    <label class="sc-pill">
                                        <input type="radio" name="da" value="@muc" checked="@(muc == "100%")" />
                                        <span>@muc</span>
                                    </label>
                                }
                            </div>
                        </div>
                    </div>

                    <!-- ---- Topping (checkbox) ---- -->
                    <div class="mb-3">
                        <div class="sc-option-title">@Resources.Strings.Detail_ToppingThem</div>
                        <div class="row g-2">
                            @foreach (var tp in TuyChonSanPham.CacTopping)
                            {
                                <div class="col-md-6">
                                    <label class="sc-topping">
                                        <input type="checkbox" name="toppings" value="@tp.Key" data-phu-thu="@tp.Value" />
                                        <span>@tp.Key</span>
                                        <span class="sc-topping-price">+@DinhDang.Tien(tp.Value)</span>
                                    </label>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- ---- Số lượng + nút mua ---- -->
                    <div class="d-flex flex-wrap align-items-center gap-3 mt-4">
                        <div>
                            <div class="sc-option-title">@Resources.Strings.Detail_SoLuong</div>
                            <div class="sc-qty">
                                <button type="button" data-buoc="giam">−</button>
                                <input type="number" name="soLuong" id="SoLuongMua" value="1" min="1" max="100" />
                                <button type="button" data-buoc="tang">+</button>
                            </div>
                        </div>

                        <div class="ms-auto text-end">
                            <div class="sc-text-muted small">@Resources.Strings.Detail_TamTinh</div>
                            <div class="sc-price-lg" id="tamTinhChiTiet">@DinhDang.Tien(sp.GiaBanThucTe)</div>
                        </div>
                    </div>

                    <div class="d-flex gap-2 mt-3">
                        <button type="submit" class="btn btn-sc btn-lg flex-fill" @(sp.SoLuongTon > 0 ? "" : "disabled")>
                            <i class="bi bi-bag-plus"></i> @Resources.Strings.Detail_ThemVaoGio
                        </button>
                        <a href="@Url.Action("Index", "GioHang")" class="btn btn-sc-outline btn-lg">
                            <i class="bi bi-bag-heart"></i> @Resources.Strings.Detail_XemGio
                        </a>
                    </div>

                    <div class="mt-3 small sc-text-muted">
                        <i class="bi bi-truck"></i> @Resources.Strings.Detail_GiaoTrong30 &nbsp;•&nbsp;
                        <i class="bi bi-shield-check"></i> @Resources.Strings.Detail_DoiLyMoi &nbsp;•&nbsp;
                        <i class="bi bi-telephone"></i> 1900 6789
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- ============ MÔ TẢ CHI TIẾT (thông tin bổ sung) ============ -->
    <div class="sc-panel mt-4">
        <ul class="nav nav-tabs mb-3" role="tablist">
            <li class="nav-item">
                <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tabMoTa" type="button">@Resources.Strings.Detail_TabMoTa</button>
            </li>
            <li class="nav-item">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabDinhDuong" type="button">@Resources.Strings.Detail_TabThongTinThem</button>
            </li>
            <li class="nav-item">
                <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tabGiaoHang" type="button">@Resources.Strings.Detail_TabGiaoHang</button>
            </li>
        </ul>

        <div class="tab-content">
            <div class="tab-pane fade show active" id="tabMoTa">
                <p>@sp.MoTa</p>
                <p class="sc-text-muted mb-0">
                    @Resources.Strings.Detail_MoTaChiTiet
                </p>
            </div>
            <div class="tab-pane fade" id="tabDinhDuong">
                <table class="table sc-table">
                    <tbody>
                        <tr><th style="width:220px">@Resources.Strings.Detail_MaSanPham</th><td>SC@(sp.MaSP.ToString("D3"))</td></tr>
                        <tr><th>@Resources.Strings.Detail_Loai</th><td>@(sp.LoaiSanPham != null ? sp.LoaiSanPham.TenLoai : "-")</td></tr>
                        <tr><th>@Resources.Strings.Detail_GiaNiemYet</th><td>@DinhDang.Tien(sp.Gia)</td></tr>
                        <tr><th>@Resources.Strings.Detail_TinhTrang</th><td>@(sp.SoLuongTon > 0 ? Resources.Strings.Detail_ConHang : Resources.Strings.Detail_TamHet)</td></tr>
                        <tr><th>@Resources.Strings.Detail_DaBan</th><td>@(string.Format(Resources.Strings.Detail_LuotBanFormat, sp.LuotBan))</td></tr>
                    </tbody>
                </table>
            </div>
            <div class="tab-pane fade" id="tabGiaoHang">
                <ul class="mb-0 sc-text-muted">
                    <li>@Resources.Strings.Detail_GiaoBanKinh5km</li>
                    <li>@Resources.Strings.Detail_PhiGiaoHang</li>
                    <li>@Resources.Strings.Detail_ThanhToanCOD</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- ============ SẢN PHẨM LIÊN QUAN (rubric 4.4) ============ -->
    @if (Model.SanPhamLienQuan.Count > 0)
    {
        <section class="sc-section">
            <div class="sc-section-head">
                <div>
                    <h2>@Resources.Strings.Detail_CoTheThich</h2>
                    <div class="sc-sub">@(string.Format(Resources.Strings.Detail_CungNhomFormat, sp.LoaiSanPham != null ? sp.LoaiSanPham.TenLoai.ToLower() : Resources.Strings.SanPham_SanPhamSuffix))</div>
                </div>
                <a href="@Url.Action("Index", "SanPham", new { maLoai = sp.MaLoai })" class="btn btn-sc-soft">
                    @Resources.Strings.Common_XemTatCa <i class="bi bi-arrow-right"></i>
                </a>
            </div>

            <div class="row g-3 g-lg-4">
                @foreach (var lq in Model.SanPhamLienQuan)
                {
                    <div class="col-6 col-md-4 col-lg-3">
                        @Html.Partial("_CardSanPham", lq)
                    </div>
                }
            </div>
        </section>
    }
</div>
